# 前端計畫：V3 Agent Run UI

日期：2026-07-08

## 目標

建立 V3 Agent workflows 的前端操作流程，不加入使用者上傳功能。

使用者應該可以：
- 提出研究問題。
- 查看研究結果。
- 從 ResearchRun 觸發 CriticReview。
- 查看 CriticReview 結果、節點狀態、事件與工具呼叫。
- 在 critic policy 建議修正時觸發 DraftRevision。
- 查看修正版答案與修正說明。

## 非目標

- 不做使用者文件上傳 UI。
- 不做使用者管理檔案庫。
- 不做公開的資料匯入流程。
- 不做 PDF 上傳或手動 evidence 上傳流程。

資料匯入維持由系統端處理，例如 backend CLI、排程任務或內部管理流程。

## 產品流程

1. 使用者送出研究問題。
2. 後端建立 ResearchRun。
3. 前端顯示研究答案與 run 狀態。
4. 使用者點擊「執行 Critic Review」。
5. 前端建立 CriticReview agent run。
6. 前端顯示整體 run 狀態、節點執行狀態、critique 結果、evidence coverage、policy decision、tool calls 與 events。
7. 如果 policy 建議修正，使用者點擊「產生修正版答案」。
8. 前端建立 DraftRevision agent run。
9. 前端顯示修正版答案、修正說明、evidence 使用情況與最終 run 狀態。

## 頁面與元件

## Research Result 頁面

在既有研究結果頁增加操作：
- 執行 Critic Review。
- 查看 Agent Runs。
- 條件符合時產生修正版答案。

頁面應顯示：
- 研究答案。
- Evidence 摘要。
- Research run 狀態。
- 關聯的 CriticReview runs。
- 關聯的 DraftRevision runs。

## Agent Run Detail 頁面

建立或擴充 agent run detail view。

顯示內容：
- Run metadata：agent type、workflow type、status、created time、completed time。
- Node timeline：node key、node type、status、started/completed time、duration、error message。
- Tool calls：tool name、status、input/output summary、error details。
- Events：event type、message、timestamp、必要時顯示 payload。

## CriticReview 結果 UI

顯示內容：
- 整體評估。
- 缺少的 evidence。
- 未被支持的 claims。
- Evidence quality notes。
- `requiresRevision`。
- `requiresMoreEvidence`。
- `routeBackTo`。
- `recommendedNextAction`。

建議 UI 行為：
- 如果 `recommendedNextAction = AcceptAnswer`，顯示成功狀態。
- 如果 `recommendedNextAction = ReviseAnswer`，顯示「產生修正版答案」。
- 如果 `recommendedNextAction = CollectMoreEvidenceThenReviseAnswer`，顯示警示狀態，並說明需要更多 evidence。
- 如果 run 失敗，顯示錯誤詳情與可行的重新執行操作。

## DraftRevision 結果 UI

顯示內容：
- 修正版答案。
- 修正說明。
- 保留的 evidence。
- 移除或弱化的 evidence。
- 仍保留的 caveats。

建議 UI 行為：
- 讓修正版答案與原始答案有明確視覺區隔。
- 保留原始答案供使用者比較。
- 顯示答案為什麼被修改。

## API 串接

使用既有後端 endpoints：
- `POST /api/research/ask`
- `GET /api/research/runs`
- `GET /api/research/runs/{runId}`
- `POST /api/agent-runs/critic-review`
- `POST /api/agent-runs/draft-revision`
- `GET /api/agent-runs`
- `GET /api/agent-runs/{runId}`

前端需要處理：
- Loading states。
- Failed runs。
- Retry 或重新執行操作。
- 沒有 agent runs 時的 empty state。
- 長時間 workflow 的 polling。

## Polling 策略

Agent runs：
- 當 status 是 `Pending` 或 `Running` 時 polling。
- 當 status 是 `Succeeded`、`Failed` 或 `Cancelled` 時停止 polling。
- 使用適中的 interval，例如 2 秒。
- 避免使用者在頁面間切換時產生重複 polling。

## UX 要求

Desktop：
- 空間足夠時，主要答案與 agent review 可以並排顯示。
- Node timeline 與 debugging details 可以折疊。

Mobile：
- 使用上下堆疊區塊。
- 主要操作要容易找到。
- 避免使用過寬表格呈現 node、tool、event 資料。

通用要求：
- 建立 run 時停用按鈕。
- 避免重複點擊造成多個 run。
- 顯示清楚錯誤訊息。
- Debugging details 要可以查看，但不要干擾一般使用流程。

## 實作順序

1. 檢查既有 Vue routes、API client 結構與 research result components。
2. 補上或更新 agent run DTO 的 TypeScript types。
3. 增加 CriticReview、DraftRevision、agent run detail 的 API client methods。
4. 增加 agent run status polling helper 或 composable。
5. 在 research result 頁加入 CriticReview action。
6. 建立 AgentRunDetail view。
7. 加上 CriticReview result rendering。
8. 加上 DraftRevision trigger 與 result rendering。
9. 補 loading、empty、error、failed-run states。
10. 執行 frontend build 與 tests。

## 驗證方式

在 `src/EquityLens.Web` 執行：
- `npm run build`
- `npm test`

手動瀏覽器檢查：
- 送出研究問題。
- 觸發 CriticReview。
- 確認 node timeline 會更新。
- 確認 policy decision 正確顯示。
- 條件允許時觸發 DraftRevision。
- 確認修正版答案正確顯示。
- 測試 backend 不可用或 run 失敗時的畫面。
- 檢查 desktop 與 mobile layout。

## 待確認問題

- AgentRun detail 要做成獨立 route，還是嵌入 research result 頁？
- 一般使用者是否應該看到完整 tool calls/events，或放在 debug toggle 後面？
- DraftRevision 要在 CriticReview 建議修正時自動啟動，還是需要使用者確認？
