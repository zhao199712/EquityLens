# Database Utilities

此目錄存放 Entity Framework Core migrations 之外的資料庫輔助腳本。

正式 schema 仍以 `src/EquityLens.Api/Migrations/` 與目前應用程式模型為主要來源；這裡的 SQL 主要用於 demo data、資料匯入與維運輔助。

- `postgres/scripts/`：PostgreSQL 輔助腳本。
